// admin.js — legacy reference file, not imported by any HTML page.
// admin-dashboard.html uses auth.js + inline scripts exclusively.
// Kept here as a reference stub; safe to delete if no longer needed.

// All shared utilities (authFetch, logoutUser, guardAuth, escapeHtml,
// reviewBadge, buildYearOptions) are now in /js/auth.js.
